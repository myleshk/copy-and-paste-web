# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push to main branch
on:
  push:
    branches:
      - main
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run build
        env:
          NEXT_PUBLIC_WEBSOCKET_PATH: ${{ vars.WEBSOCKET_PATH }}
          NEXT_PUBLIC_TURN_SERVER_URL: ${{ vars.TURN_SERVER_URL }}
          NEXT_PUBLIC_TURN_SERVER_USERNAME: ${{ vars.TURN_SERVER_USERNAME }}
          NEXT_PUBLIC_TURN_SERVER_PASSWORD: ${{ secrets.TURN_SERVER_PASSWORD }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_COPY_AND_PASTE_BD83A }}
          projectId: copy-and-paste-bd83a
          channelId: live
